@import galen-extras/galen-extras-rules.gspec

@set
    minimal                     -1 to 1px
    minimal_margin              ${minimal}

    tiny                        0 to 5px
    tiny_margin                 ${tiny}

    small                       0 to 15px
    small_margin                ${small}

    normal                      15 to 35px
    normal_margin               ${normal}
